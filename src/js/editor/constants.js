// Tool modes
export const ToolMode = {
  SELECT: "select",
  MAJOR_STATION: "major_station",
  MINOR_STATION: "minor_station",
  NEW_TRACK: "new_track",
};

// Default draw settings
export const DEFAULT_DRAW_SETTINGS = {
  text: true,
  fast: false,
  calcTextPositions: true,
  minorStationText: true,
  fontSize: 14,
  minorFontSize: 10,
};
